;;;;======================= general settings ================================
(setq-default truncate-lines t)
(setq-default auto-fill-mode nil)

;; ======================= navigation ================================
;; iswitch -- for switching buffers
  (iswitchb-mode 1)
;(setq iswitchb-buffer-ignore '("^ " "*Buffer"))


;; ======================= Set ~ file location ================================
(setq make-backup-files nil) ;; do not make backup files
