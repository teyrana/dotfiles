# git config file
# dwilliams, 2019

[alias]
        br = branch
        co = checkout
        ea = submodule foreach
        sta = show --stat
        stat = show --stat
        st = status
        ls = ls-files
        sub= submodule
        pf = push --force-with-lease

[user]
        name = Daniel Williams
        email = dwilliams@nextdroid.com
        
[core]
        autocrlf = input
        filemode = false
        editor = emacs
	excludesfile = ~/.gitignore

[push]
	default = simple

[merge]
	commit = no
	ff = only

[log]
	abbrevcommit = true
        decorate = auto

[format]
	pretty = oneline

# [receive]
#	denyCurrentBranch = updateInstead
# [status]
#	submodulesummary = 1
#
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
